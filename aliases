alias l="ls -l"
alias lf="ls | fzf"


alias btui="bluetuith"

alias j="source ~/.scripts/_j"

alias t="task"
alias ta="task add"
alias tl="task list"
alias td="task done"
alias tdel="task delete"


alias tx="tmuxinator"

# TODO change xdg-open to mimeo
# TODO integration with fasd / zoxide
alias open="xdg-open"
alias o='xdg-open'

# fzf-marks
# space for auto completion
alias m="fzm"


alias n3="nnn"

alias p="python3"

# Git
# TODO: change to git aliases
alias lg="lazygit"
alias g="git"
alias gc="git commit"
alias ga="git add"
alias gitlabci="git add .gitlab-ci.yml && git commit -m 'Updated
.gitlab-ci.yml' && git push"

# Copy stdin to the clipboard
alias xc="xclip -selection clipboard"

# vim
alias v="nvim"
alias vim="nvim"

# Wireguard
alias wgup="sudo wg-quick up"
alias wgdw="sudo wg-quick down"

# Docker
alias d="docker"
alias dc="docker-compose"
alias dcup="docker-compose up"
alias dcupd="docker-compose up -d"
alias dcdw="docker-compose down"

# Terraform
alias tf="terraform"
alias tfa="terraform apply -auto-approve"
alias tfp="terraform plan"
alias tfd="terraform destroy"

# Kubernetes
alias k="kubectl"


# Vagrant
# Vagrant is aliased to vg to auto determine whether to use 
# vagrant or vagrant_libvirt
alias vagrant="vg"
alias vgssh="vagrant ssh"
alias vgup="vagrant up"
alias vgupp="vagrant up --provision"
alias vgupnp="vagrant up --no-provision"
alias vgh="vagrant halt"
alias vgd="vagrant destroy"

# Virtualbox
alias vb="VBoxManage"


# Cursor
alias ca="~/.local/bin/distrobox_exported/cursor-agent"
